# institutions/uslugi/services/ — Service registry and application logic.
#
# registry.py    — Maps function names to portal apsUniqueName identifiers.
# apply_service.py — Core apply/get flow using the authenticated_client.
