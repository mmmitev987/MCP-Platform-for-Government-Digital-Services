# institutions/uslugi/auth/ — Authentication layer for uslugi.gov.mk
#
# Each module here instantiates a shared/ class with uslugi-specific config.
# The resulting singleton objects are imported by the tools and client layers.
