# institutions/mon/auth/ — Authentication layer for e-uslugi.mon.gov.mk
#
# Each module instantiates a shared/ class with MON-specific config.
# The resulting singletons are imported by the tools and client layers.
